# ~/.config/qtile/themes/pywal.py
# Pywal dynamic theme colors
from .pywal_colors import colors  # Import colors from pywal_colors.py

pywal_border_active    = colors[2]  # Green (active window border)
pywal_border_inactive  = colors[8]  # Gray (inactive window border)
pywal_bar              = colors[0]  # Black (bar background)
pywal_active_window    = colors[10] # Light green (active window text)
pywal_inactive_window  = colors[8]  # Gray (inactive window text)
pywal_urgent           = colors[1]  # Red (urgent notifications)
pywal_border_bg        = colors[0]  # Black (border background)
pywal_title_text       = colors[7]  # White (title text)
pywal_text             = colors[7]  # White (general text)
pywal_highlight        = colors[4]  # Blue (highlights)
